Creates and manages a Scaleway Mailbox domain.

A domain is required before mailboxes can be provisioned. After creation, configure
the exposed DNS records in your DNS zone so the domain can be validated.
